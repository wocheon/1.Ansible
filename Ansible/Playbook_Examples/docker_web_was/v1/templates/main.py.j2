from fastapi import FastAPI

app = FastAPI(title="{{ fastapi_title }}")


@app.get("/")
def root():
    return {
        "message": "{{ fastapi_message }}",
        "backend": "FastAPI",
    }


@app.get("/health")
def health():
    return {
        "status": "ok",
    }
